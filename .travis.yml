os: linux
dist: trusty
sudo: required


addons:
  apt:
    packages:
      - gcc-multilib
      - cmake

script: 
  - chmod +x CI_scripts/*
  - CI_scripts/get_compiler.sh
  - CI_scripts/get_includes.sh
  - CI_scripts/compile.sh gamemode.pwn